{
  "name": "scrapcraft",
  "version": "0.2.0",
  "description": "A voxel scrapyard game where middle schoolers build robots, program them with AI, and learn embedded engineering.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node src/maker/__tests__/run-tests.mjs",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
